`best_epoch=23` ✅，Week 6 全部完成。

---

# Week 6 完整总结

六天从零搭建了一个完整的深度学习工程 Pipeline：

```
数据层                模型层              训练层              工程层
──────────────────────────────────────────────────────────────────
FASTA 解析            GELU 激活           Early Stopping      GPU 自动选择
Dataset/DataLoader    Transformer         Warmup + Cosine     Checkpoint 保存
序列截断/对齐         Encoder             学习率调度          训练日志 CSV
训练/验证集划分       二级结构分类头      完整训练循环        推理脚本
```

| Day | 核心内容 | 关键代码 |
|-----|---------|---------|
| 1 | FASTA 解析 + DataLoader | `parse_fasta` / `collate_fn` |
| 2 | 截断 + 数据集划分 | `ProteinDatasetV2` / `random_split` |
| 3 | GELU + Early Stopping | `nn.GELU()` / `EarlyStopping` |
| 4 | 学习率调度 | `LinearLR` + `CosineAnnealingLR` |
| 5 | GPU + Checkpoint + 日志 | `.to(device)` / `torch.save` / `csv` |
| 6 | 推理脚本 | `model.eval()` + `torch.no_grad()` |

